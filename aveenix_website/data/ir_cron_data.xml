<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Background image downloader: pulls the first external image URL into the
            native product image field for products flagged 'pending'. Independent
            of WooCommerce — uses the URL already stored on the product. Throttled,
            batched, self-resuming so it never blocks the catalog pull. -->
        <record id="cron_fetch_external_images" model="ir.cron">
            <field name="name">Aveenix: Download External Product Images</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model.cron_fetch_external_images(batch_size=50, timeout=10)</field>
            <field name="interval_number">10</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="False"/>
        </record>
        <record id="cron_generate_ai_category_assets" model="ir.cron">
            <field name="name">Aveenix: Generate AI Icons &amp; Images for Categories</field>
            <field name="model_id" ref="website_sale.model_product_public_category"/>
            <field name="state">code</field>
            <field name="code">model.cron_generate_ai_assets()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="False"/>
        </record>
    </data>
</odoo>
